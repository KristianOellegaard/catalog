Depends on sorl-thumbnail and dbsettings (i forked dbsettings to make it work with the newest django version here: https://github.com/KristianOellegaard/django-dbsettings)

Furthermore you need to add these to context processors:

"catalog.context_processors.basket",
"catalog.context_processors.categories",

Media files are *now* included. The webshop is 100% english but translated to Danish with the built in tools from django.
